<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoAnMobile"
        tools:targetApi="34">
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.register"/>
        <activity android:name=".activity.homepage"/>
        <activity android:name=".activity.minigamepage" />
        <activity android:name=".activity.minigame_quiz" />
        <activity android:name=".activity.minigame_correct" />
        <activity android:name=".activity.player"/>
        <activity android:name=".activity.player_name"/>
        <activity android:name=".activity.correct_highscore"/>
        <activity android:name=".activity.correct_result"/>
        <activity android:name=".activity.correct_test"/>
        <activity android:name=".activity.quiz_highscore"/>
        <activity android:name=".activity.quiz_result"/>
        <activity android:name=".activity.quiz_test"/>
    </application>

</manifest>